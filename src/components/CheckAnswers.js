import styled from "styled-components";

export default function CheckAnswers({stateCheck}){
    return(
        <CheckAnswer>
        
        </CheckAnswer>
    );
}

const CheckAnswer = styled.div`
    
`;